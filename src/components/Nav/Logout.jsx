import { Button } from 'primereact/button';

const Logout = () => {
  const handleLogout = () => {};

  return (
    <Button
      rounded
      onClick={handleLogout}
    >
      Log Out
    </Button>
  );
};

export default Logout;
